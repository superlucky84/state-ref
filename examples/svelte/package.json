{
  "type": "module",
  "name": "stateref-example-svelte",
  "version": "0.0.0",
  "private": true,
  "description": "Svelte demo for the state-ref server-sync manual checklist",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@stateref/connect-svelte": "workspace:*",
    "@stateref/sync": "workspace:*",
    "state-ref": "workspace:*",
    "stateref-example-shared": "workspace:*",
    "svelte": "^4.2.19"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "svelte-check": "^3.8.6"
  }
}
